<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <!-- REQUIRED: Basic networking pipelines to load web endpoints -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- REQUIRED: Hardware camera lens access framework for your ScannerActivity -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Informs app stores that camera availability won't block device downloads entirely -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Floriledia"
        tools:targetApi="31">
        
        <activity 
            android:name=".views.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".views.RegisterActivity" android:exported="false" />
        <activity android:name=".views.FlorilegiumActivity" android:exported="false" />
        <activity android:name=".views.ScannerActivity" android:exported="false" />
        <activity android:name=".views.AccountActivity" android:exported="false" />
        <activity android:name=".views.ResultScreenActivity" android:exported="false" />
        <activity android:name=".views.PlantDetailsActivity" android:exported="false" />

    </application>

</manifest>